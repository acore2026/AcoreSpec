---
"@acore2026/openspec": minor
---

### New Features

- **6G prototype schema** — Add a built-in `prototype-driven` workflow covering demo brief, repository/device inventory, end-to-end scenario, integration contracts, design, runbook, acceptance evidence, and repository-scoped tasks.
- **Prototype profile** — Add `openspec config profile prototype` and `openspec init --profile prototype`.
- **Prototype actions** — Generate `/opsx:survey`, `/opsx:integrate`, `/opsx:rehearse`, and `/opsx:demo` skills and commands for supported AI tools.

### Other

- Add a Chinese field guide grounded in multi-repository core-network, transport, Android/AR, robot, container, observability, and live-demo practices.
